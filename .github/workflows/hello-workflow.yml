name: Hello
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - run: echo "Hello"
      env:
        TAG: ${REF##*/}
        REF: ${{ github.ref }}
        GITHUB_CONTEXT: ${{ toJson(github) }}
